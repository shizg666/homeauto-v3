<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.landleaf.homeauto.center.device.model.mapper.CategoryAttributeMapper">

    <select id="getAttributesByCategoryIds"
            resultType="com.landleaf.homeauto.common.domain.vo.category.CategoryAttributeVO">
        SELECT
        ad.ID,
        ad.NAME,
        ad.nature,
        ca.category_id as categoryId
        FROM
        category_attribute ca,
        home_auto_attribute_dic ad
        WHERE
        ca.attribute_id = ad.ID
        AND ca.category_id IN
        <foreach collection="categoryIds"  item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>
</mapper>
